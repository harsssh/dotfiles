{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "(Edit|Write)",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATH\" == *.nix ]]; then nixfmt \"$CLAUDE_FILE_PATH\"; fi"
          }
        ]
      }
    ]
  }
}
